(function(){'use strict';const revealEls=document.querySelectorAll('[data-reveal]');if('IntersectionObserver'in window&&revealEls.length){const io=new IntersectionObserver((entries)=>{entries.forEach((e)=>{if(e.isIntersecting){e.target.classList.add('in');io.unobserve(e.target);}});},{threshold:0.12,rootMargin:'0px 0px -40px 0px'});revealEls.forEach((el)=>io.observe(el));}else{revealEls.forEach((el)=>el.classList.add('in'));}
const fakeInput=document.querySelector('[data-fake-input]');if(fakeInput){const prompts=['Cinematic drone shot of Tokyo at golden hour, anamorphic lens','Editorial product photo of a glass perfume bottle, soft volumetric light','Lo-fi 9:16 reel for a coffee brand, warm grain, slow zoom','Talking head explainer with my AI avatar in a studio set','60-second voiceover ad for a fitness app, hype tone, ElevenLabs voice','Logo for an indie game studio called "Nova Drift" — vector, neon',];let pi=0;let ci=0;let deleting=false;function tick(){const cur=prompts[pi];if(!deleting){ci++;fakeInput.textContent=cur.slice(0,ci);if(ci>=cur.length){deleting=true;setTimeout(tick,1900);return;}
setTimeout(tick,28+Math.random()*50);}else{ci-=2;if(ci<=0){ci=0;deleting=false;pi=(pi+1)%prompts.length;}
fakeInput.textContent=cur.slice(0,ci);setTimeout(tick,14);}}
tick();}
const genCards=document.querySelectorAll('.gen-card[data-styles]');const cdh=document.querySelector('[data-cd-h]');const cdm=document.querySelector('[data-cd-m]');const cds=document.querySelector('[data-cd-s]');if(cdh&&cdm&&cds){function pad(n){return n<10?'0'+n:''+n;}
function update(){const now=new Date();const target=new Date(now);target.setHours(23,59,59,999);let diff=Math.max(0,target-now);const h=Math.floor(diff/3600000);diff-=h*3600000;const m=Math.floor(diff/60000);diff-=m*60000;const s=Math.floor(diff/1000);cdh.textContent=pad(h);cdm.textContent=pad(m);cds.textContent=pad(s);}
update();setInterval(update,1000);}
const spotEl=document.querySelector('[data-spots]');if(spotEl){let n=parseInt(spotEl.textContent,10)||47;setInterval(()=>{if(Math.random()<0.18&&n>8){n-=1;spotEl.textContent=n;}},9000);}
const liveEl=document.querySelector('[data-live-signups]');if(liveEl){const names=['Maya from Austin, TX','Daniel from Toronto','Aisha from London','Marcus from Brooklyn','Priya from Singapore','Liam from Dublin','Carla from São Paulo','Yuki from Osaka','Eli from Tel Aviv','Naledi from Cape Town','Rafael from Madrid','Anika from Toronto','Tomás from Mexico City','Isabella from Sydney','Jonas from Berlin',];function show(){const name=names[Math.floor(Math.random()*names.length)];liveEl.innerHTML='<span class="dot"></span><span><b>'+name+'</b> just joined</span>';liveEl.classList.add('show');setTimeout(()=>liveEl.classList.remove('show'),4200);}
setTimeout(show,4500);setInterval(show,11000+Math.random()*6000);}
document.addEventListener('click',(e)=>{const a=e.target.closest('a[href^="#"]');if(!a)return;const id=a.getAttribute('href');if(id.length<2)return;const t=document.querySelector(id);if(!t)return;e.preventDefault();t.scrollIntoView({behavior:'smooth',block:'start'});});const pwd=document.querySelector('[data-password]');if(pwd){const meterEl=document.querySelector('[data-pw-meter]');pwd.addEventListener('input',()=>{if(!meterEl)return;const v=pwd.value||'';let score=0;if(v.length>=8)score++;if(/[A-Z]/.test(v))score++;if(/\d/.test(v))score++;if(/[^A-Za-z0-9]/.test(v))score++;const pct=(score/4)*100;meterEl.style.width=pct+'%';meterEl.style.background=score<=1?'#ff5b6c':score===2?'#ffb547':score===3?'#a3ff66':'linear-gradient(90deg,#a3ff66,#38e1ff)';});}})();